---
description: 修改 PPT 刷新/写盘相关代码后建议编译并验证
alwaysApply: true
---

# 演示文稿相关改动验证

在本仓库中**只要改动了会影响 `pptrefresh` 包下文档写回（`document`）、编排（`orchestration`）、`RefreshSampleMain` 或根目录 `template_*.pptx` 模板引用的 Java**，在结束该轮修改前**建议由助手自行执行**：

```powershell
Set-Location d:\IdeaProjects\java_ppt\autoppt; mvn -q compile test
```

完整刷新需 LLM（`DASHSCOPE_API_KEY` 等），可选：

```powershell
mvn -q exec:java "-Dexec.mainClass=com.example.pptrefresh.sample.RefreshSampleMain"
```

输入样例：`samples/20260430-偏债混-M1.pptx`；输出：`samples/20260430-偏债混-M1-refreshed.pptx`。Spring Boot API 用 `mvn -q spring-boot:run` 验证。
